name: Run Tests

on: push

jobs:
  run_tests:
    name: Run Tests
    runs-on: ubuntu-latest
    container:
      image: perl:5.28.1
    steps:
      - uses: actions/checkout@v3
      - run: prove -r t
